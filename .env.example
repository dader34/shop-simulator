# Copy to .env.local and fill in. .env.local is gitignored.
# The web app does NOT read this file — it uses the in-app API key box
# (browser localStorage). This is here only for any local tooling you add.
ANTHROPIC_API_KEY=sk-ant-...
